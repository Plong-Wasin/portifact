# Restore artifacts as private

**Status: accepted.** Restoring an Artifact from Trash restores its content and Explicit access grants but resets General access to `only people with access`. This prevents an Artifact that was previously reachable through a broad audience from becoming visible again merely because it was restored, while preserving deliberate person-to-person access.
